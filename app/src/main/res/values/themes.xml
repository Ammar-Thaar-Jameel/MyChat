<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyChat" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/app_background_color</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="TextVeiwStyle" parent="Widget.AppCompat.TextView">
        <item name="android:background">@drawable/my_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:padding">16dp</item>


    </style>
    <style name="TextVeiwStyle.revers" parent="Widget.AppCompat.TextView">
        <item name="android:background">@drawable/my_background_revers</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
        <item name="android:padding">16dp</item>
    </style>
    <style name="TextVeiwStyle.EditText" parent="Widget.AppCompat.TextView">
        <item name="android:background">@drawable/my_background_textview</item>
        <item name="android:textSize">14sp</item>
        <item name="android:padding">24dp</item>
    </style>
</resources>